image: node:8
pipelines:
  default:
    - step:
        name: build
        script:
          - npm install
          - npm install -g jest
    - step:
        name: anagrams
        script:
          - npm install
          - npm install -g jest
          - jest anagrams